# פריסה

האתר בנוי כך שאותו commit נבנה נכון גם ב-Vercel וגם ב-GitHub Pages. ההבדל היחיד הוא שני משתני סביבה, כך שאפשר להקים את Vercel בלי להפיל את האתר הקיים.

| משתנה | Vercel | GitHub Pages |
|---|---|---|
| `VITE_BASE_PATH` | לא מוגדר (ברירת מחדל `/`) | `/Ayal-Taarog/` |
| `VITE_SITE_URL` | הכתובת המלאה של האתר | `https://havivmoshe-git.github.io/Ayal-Taarog` |

`VITE_SITE_URL` **חובה**. בלעדיו תגיות ה-Open Graph יוצאות יחסיות, ותצוגה המקדימה בוואטסאפ תישבר — וזו הסיבה המרכזית שאנשים לוחצים על הקישור. הבנייה תדפיס אזהרה אם הוא חסר.

---

## הקמת Vercel

חד־פעמי, כ-10 דקות.

### 1. חיבור הריפו

1. https://vercel.com → הרשמה **עם חשבון GitHub** (זה חוסך את שלב ההרשאות).
2. **Add New → Project**.
3. ליד `havivmoshe-git/Ayal-Taarog` → **Import**.
4. Vercel יזהה Vite לבד. **אל תשנו** את הגדרות הבנייה — `vercel.json` כבר מגדיר אותן.

### 2. הגדרת משתני הסביבה

לפני שלוחצים Deploy, לפתוח **Environment Variables** ולהוסיף:

| Name | Value |
|---|---|
| `VITE_SITE_URL` | `https://ayal-taarog.vercel.app` |

(אם תחברו דומיין משלכם בהמשך — עדכנו את הערך הזה לכתובת החדשה ופרסו מחדש.)

**וגם שני אלה — בלעדיהם העריכות לא יופיעו באתר:**

| Name | Value |
|---|---|
| `VITE_SUPABASE_URL` | `https://jgrpwwkpfnykapkmefqp.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImpncnB3d2twZm55a2Fwa21lZnFwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODYwOTk1NTcsImV4cCI6MjEwMTY3NTU1N30.w4muiqbkV68k1u4WsIrPo8ubEB56-dMyROjVWRIj5Rk` |

מפתח ה-`anon` נמצא כאן בכוונה — הוא מיועד לחשיפה פומבית ומגיע ממילא לכל מבקר בתוך קוד האתר. נבדק בפועל שהוא **לא** מאפשר כתיבה: ניסיון עריכה איתו לא שינה דבר, וניסיון העלאת קובץ הוחזר עם `403 row-level security policy`.

### 3. Deploy

ללחוץ **Deploy**. תוך כדקה תקבלו כתובת.

### 4. בדיקה

- האתר נטען ונראה תקין
- `/#/admin` פותח את הפאנל
- הדבקת הכתובת בוואטסאפ מציגה תצוגה מקדימה **עם תמונה** — אם התמונה חסרה, `VITE_SITE_URL` שגוי או לא הוגדר

מכאן כל push לענף פורס אוטומטית.

---

## דומיין משלכם

**Settings → Domains** → להוסיף את הדומיין. Vercel יראה אילו רשומות DNS להוסיף אצל ספק הדומיין.

אחרי שהדומיין פעיל — לעדכן את `VITE_SITE_URL` לכתובת החדשה ולפרוס מחדש, אחרת תגיות השיתוף עדיין יצביעו לכתובת הישנה.

---

## GitHub Pages

ממשיך לעבוד במקביל ללא שינוי, כגיבוי. אחרי ש-Vercel יציב אפשר לכבות אותו: למחוק את `.github/workflows/deploy.yml`, או להשאיר — הוא לא מפריע ולא עולה כסף.

---

## בנייה מקומית

```bash
npm install
npm run images   # פעם אחת — מייצר את תמונות הגלריה
npm run dev
```

לבדיקת בנייה כמו בפרודקשן:

```bash
VITE_SITE_URL=http://localhost:4173 npm run build && npm run preview
```

---

## הערה על `vercel.json`

הקובץ הוא JSON ולכן **אין בו הערות** — Vercel מאמת אותו מול סכימה שבה `additionalProperties: false`, כך שכל מפתח לא מוכר פוסל את הקובץ כולו והפריסה נכשלת עוד לפני שהבנייה מתחילה. ההסבר על ההגדרות נמצא כאן במקום:

- **`/assets/*`** — שמות הקבצים כוללים חתימה שמשתנה בכל בנייה, ולכן אפשר לשמור אותם במטמון לנצח.
- **`/gallery/*`** — נוצרות בכל בנייה בשמות קבועים, ולכן מטמון של יום עם אימות מחדש.
- **`/`** — ללא מטמון. אחרת עריכה שפורסמה עלולה לשבת מאחורי גרסה ישנה של האתר.

---

## היסטוריה, נתונים וניטור

### הרצה חד-פעמית ב-Supabase

`supabase/insights.sql` — SQL Editor → New query → Run. אפשר להריץ שוב בבטחה.
(כבר הורץ על הפרויקט `ayal-taarog`.)

הוא יוצר:

| טבלה | מה יש בה | מי יכול לקרוא |
|---|---|---|
| `content_versions` | כל פרסום, שחזור ותצלום תקופתי של הטיוטה | המשתמש המחובר בלבד |
| `events` | כניסות, מקטעים שנצפו, לחיצות, עומק גלילה | המשתמש המחובר בלבד |
| `lead_intents` | פניות שמולאו — כולל כאלה שלא נשלחו | המשתמש המחובר בלבד |

ושתי פונקציות: `site_insights(days)` שמסכמת הכול בשאילתה אחת, ו-`record_lead(...)`
שדרכה האתר כותב פנייה. הפונקציה השנייה קיימת מסיבה ספציפית — upsert רגיל דרך
PostgREST מתרגם ל-`ON CONFLICT DO UPDATE`, שחייב לקרוא את השורה המתנגשת, ולתת
למבקרים הרשאת קריאה על הטבלה הזו פירושו לתת לכל מבקר את פרטי הקשר של כל האחרים.

מבקר יכול רק לכתוב. `select` מהטבלאות האלה עם מפתח ה-`anon` מחזיר רשימה ריקה.

### מפתח ה-anon נמצא עכשיו גם בחבילת האתר הציבורי

עד עכשיו הוא היה רק ב-chunk של הפאנל. המדידה רצה בדפדפן של המבקר, ולכן היא
צריכה אותו. זו ההתנהלות הרגילה ב-Supabase: המפתח מיועד לחשיפה, וההגנה היא
ב-RLS — שנבדק, ולא רק הונח.

### ניטור

`.github/workflows/monitor.yml` מריץ את `scripts/monitor.mjs` כל ארבע שעות.
ריצה שנכשלת שולחת מייל לבעל המאגר; זו ההתראה.

הבדיקות: זמינות ומהירות האתר, שלמות ה-HTML, משקל החבילה מול תקציב, פרץ של 25
בקשות בו-זמנית עם חציון ו-p95, זמינות ותקינות `content.json`, ערות של Supabase,
ושמירה על כך שכתיבה אנונימית נחסמת. הריצה עצמה משמשת גם כ-keep-alive — התוכנית
החינמית משהה פרויקט שבוע ללא תנועה.

הרצה מקומית:

```bash
SUPABASE_URL=... SUPABASE_ANON_KEY=... node scripts/monitor.mjs
```

---

## דף הפידבק

`supabase/feedback.sql` — הרצה חד-פעמית ב-SQL Editor. (כבר הורץ.)

הקישור לשליחה לאורחים: **`/feedback`** — נתיב נקי, בלי `#`, כי הוא נשלח בוואטסאפ
לאדם אמיתי. הוא עובד גם כ-`#/feedback` בכל אירוח שאין בו rewrite.

הטופס שולח דרך `submit_feedback`, פונקציה בהרשאות בעלים. הטבלה עצמה סגורה
לחלוטין למפתח הציבורי — אי אפשר לקרוא ממנה, אי אפשר לכתוב אליה ישירות, ואי
אפשר להגיע דרכה עם `status` מוכן מראש. הפונקציה אוכפת דירוג בטווח, אורכי טקסט,
ותקרה של חמש שליחות בדקה.

**שני שדות טקסט, וזו הנקודה:** `quote` הוא מה שהאורח מוכן שיפורסם, ו-`private_note`
מגיע לבעלים בלבד. אין בפאנל שום כפתור שמעביר את השני לאתר.

אישור בפאנל מוסיף את הציטוט למקטע הפידבקים **כטיוטה** — הוא עובר את אותו מסך
"לפני פרסום" כמו כל שינוי אחר, ואפשר לבטל אותו מההיסטוריה. אם אין עדיין מקטע
פידבקים, האישור הראשון יוצר אותו, ממוקם לפני טופס יצירת הקשר.
