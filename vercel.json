{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run images && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "headers": [
    {
      "comment": "Fingerprinted build output — safe to cache forever.",
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    },
    {
      "comment": "Generated from assets/source on every build; the filenames are stable, so revalidate.",
      "source": "/gallery/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=86400, must-revalidate" }]
    },
    {
      "comment": "The shell must never be stale, or a published edit could sit behind an old bundle.",
      "source": "/",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }]
    }
  ]
}
